---
name: multimodal-upgrade
description: 把文本模型升级为完整的多模态助手（看图、识视频、生成图片、生成视频）。当用户要求识别图片/视频、生成图片/海报/视频、搭建或接入多模态能力、提供 baseURL 与 API Key 想接入视觉/生成模型，或提到"看图、作画、视频分析、文生图/图生图/文生视频"时使用——即使没明说"多模态"。
---

# 文本模型 → 多模态：全套施工流程

## 使命

在不更换主模型（当前文本模型）的前提下，通过"委托 + 工具链"从零构建四大能力：
1. **图片识别**（看图）　2. **图片生成**（作画：文生图/图生图/多图合成）　3. **视频识别**（识视频、多步分析）　4. **视频生成**（拍视频：文生视频/图生视频/关键帧）

所有生成结果尽量在聊天窗口内联预览；完成后把机制固化为全局协议，后续会话直接复用。

**架构总纲**：主模型 = 大脑（理解/规划/写提示词/协调/整理输出）；专门多模态模型 = 感官与手（感知与创作）；一个统一委托通道 = 神经通道。**你不亲自"看图/作画"**。

## 流程速览

1. **能力自检**：盘点你的工具（终端/文件系统/网络），选定执行模式（见下）
2. **先查后要**：先检测环境（Agent 配置）已有模型，能覆盖的能力优先复用；未覆盖的才向用户要 baseURL + API Key
3. **安全承诺**：用户给密钥时先逐条声明安全处理方式（绝不外泄、只存本地、全程脱敏、可随时撤销）
4. **自动匹配**：拉模型清单 → 能力覆盖检查 → 逐能力匹配 → 纯色探测/最小请求实测 → 输出《能力-模型匹配表》→ 用户拍板
5. **阶段施工**：委托通道 → 四能力逐个接入（每个完成"真实往返验证"：生成 → 回读识别 → 对比 → 用户确认）
6. **展示规范**：media_out/ 相对路径嵌入、PIL 校验、缩放、视频 faststart 转码
7. **固化交付**：协议入全局配置 + 冒烟脚本 + 交付报告

## 执行模式（每步按当前模式执行）

- **A 档 · 全能**（有终端 + 文件系统 + 网络）：自建委托脚本，全自动施工
- **B 档 · API**（无终端/文件系统，但有网络）：用内置联网能力直接调 API，素材用 Base64/URL 传递
- **C 档 · 指令**（无任何工具）：产出施工包（脚本源码 + 命令 + 验证方法）交用户执行，用户回报输出后继续指导

## 模型来源：先查后要（详细规则见 references/matching.md）

1. 扫描 Agent 配置（`~/.zcode/`、`v2/config.json`、`config/models`、`.env`、环境变量等）列出已有模型
2. 逐模型能力探测：识别类用"纯色探测法"（`scripts/vision_probe.py` 生成 64×64 纯色 PNG 问颜色，答对才算能看图）；生成类按命名/文档判断，拿不准发最小请求
3. 已有模型能覆盖的能力**优先建议用户使用**（零成本、无需新密钥）；输出《环境已有模型能力表》请用户确认
4. 未覆盖的能力：向用户要对应供应商的 **baseURL + API Key**（可多个供应商；用户也可直接指定模型）
5. **部分支持照常交付**：供应商只支持一/两/三种能力时，支持哪些就接入哪些，不支持的如实标注（"待配置"= 未提供，"不支持"= 供应商无此能力），不阻塞其他能力

## 密钥安全承诺（用户提供密钥时，先逐条声明再分析）

1. **绝不外泄**：密钥只用于调用用户指定的供应商 API，不发送给任何第三方
2. **只存本地凭据文件**：如 `~/.zcode/keys.json` 或项目 `.env`，设置仅当前用户可读的权限；不写进聊天上下文、不写进任何文档
3. **全程脱敏**：所有输出/日志/报错中密钥显示为掩码（`sk-***abcd`）；`--dry-run` 打印请求体时同样脱敏
4. **不写入生成物**：脚本/协议/README/交付报告一律不含密钥，代码从凭据文件/环境变量读取、不硬编码
5. **可随时撤销**：用户可随时要求删除凭据文件或更换密钥；删除后对应能力回到"待配置"

## 阶段施工单（详细见 references/steps.md）

- **阶段 0 委托通道**：统一脚本（可基于 `scripts/delegate_skeleton.py`），子命令契约 `recognize / imagegen / videorecognize / videogen`；**适配器模式**与模型解耦（换模型只换适配器+配置）；素材自动转 Data URL；结果存当前工作区 `media_out/`；网络调用带超时；统一错误格式；支持 `--dry-run`
- **阶段 1 图片识别**：OpenAI 兼容 → `chat/completions` 多模态消息（text + image_url Data URL）；纯色探测通过后，用真实图片往返验证
- **阶段 2 图片生成**：`/images/generations`；`b64_json` 必须放 `extra_body` 内（顶层无效）；返回兼容 b64/url 两种；图生图/多图合成传 `extra_body.image`；生成后回读识别对比
- **阶段 3 视频识别**：chat 接口 `video_url` 内容块；`--max-tokens` 8000+；复杂分析加 `thinking`（若模型支持）；视频 >80MB 先压缩
- **阶段 4 视频生成**：任务式三步（提交 → 轮询 → 下载）；`num_frames` ≤ 上限且满足 8n+1（如 81/121/241/441，时长 = 帧数 ÷ fps，推荐 24fps）；限流/队列满退避重试；多域名时选带存储桶的端点（先实测一单）；下载失败如实报告任务 id 与地址
- **阶段 5 展示规范**：见 `references/display.md`（`media_out/` 相对路径、PIL verify、>3MB 或宽 >2000px 缩放 1024、视频用 Markdown 图片语法 + moov 前置 faststart + 体积 < 实测上限）
- **阶段 6 客户端内联渲染**：默认不支持才修补（Electron 套路 + 自愈机制），见 `references/display.md`
- **阶段 7 固化**：协议（含模型配置表，**不含密钥**）写入持久化配置（AGENTS.md/CLAUDE.md 等）；冒烟脚本一键检查；README；自愈演练

## 全局铁律

1. **委托分工**：识别/生成一律走已确认的专门模型，主模型只负责理解、规划、写提示词、整理结果
2. **先查后要**：环境已有模型能覆盖的能力，不得先向用户索要凭据
3. **密钥安全**：见上节承诺，任何环节不违反（只存本地、全程脱敏、不写入生成物）
4. **诚实**：失败如实报告（含任务 id），严禁谎称成功、严禁用未配置模型顶替；客户端不支持预览就明说并给替代方案
5. **验证闭环**：每接入一个能力必须做"真实往返测试"并让用户确认"显示正常吗"
6. **结果展示**：生成结果尽量在聊天窗口内联预览，不能只给路径了事
7. **不猜格式**：接口格式不明时向用户索要文档/curl 示例，`--dry-run` 验证后固化

## 遇到问题时先查 references/pitfalls.md（踩坑清单）

常见：绝对路径破图、大图破图、`<video>` 被过滤、moov 末尾卡"加载中"、预览上限凭猜测（要实测）、多域名选错下载不到、队列满/限流、下载中断坏文件、凭据缺失、接口格式不明、忽略环境已有模型。

## 完成定义（全部满足才算完工）

1. 环境已有模型已完成检测并优先复用；不足部分凭据补齐；**支持的全部能力均已接入且可用**（"待配置"= 未提供，"不支持"= 供应商无此能力，如实标注）
2. 每个已接入能力完成一次真实往返验证（生成 → 回读 → 对比）
3. 图片/视频按当前客户端能力展示并获用户确认（或如实告知不支持 + 替代方案）
4. 客户端补丁（如适用）自愈机制验证通过；冒烟脚本通过
5. 协议（含模型配置表，不含密钥）已写入持久化配置；用户更换模型仅需更新配置表
6. 提交《交付报告》：已接入能力清单（标注来源：环境已有模型/用户新提供）、待配置/不支持清单（含原因）、工具清单、全部用法、踩坑清单、密钥安全说明、环境限制

## 资源

- `references/matching.md` — 模型来源全流程（检测环境已有模型 → 要凭据 → 安全承诺 → 自动匹配 → 用户拍板）
- `references/steps.md` — 阶段 0~4、7 施工细节与提示词撰写规范
- `references/display.md` — 阶段 5~6 展示规范与客户端内联渲染补丁
- `references/pitfalls.md` — 踩坑清单（遇到问题先查它）
- `scripts/vision_probe.py` — 纯色探测图生成器（验证模型能否看图）
- `scripts/delegate_skeleton.py` — 委托脚本骨架（适配器模式，与模型解耦）
- `assets/tables.md` — 《环境已有模型能力表》《能力-模型匹配表》模板
